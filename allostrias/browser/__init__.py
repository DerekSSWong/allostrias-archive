"""The item browser: every named Legendary and Epic, in the game's own frame.

    python3 -m allostrias.browser.build      # cache/browser/bundle.json
    python3 -m allostrias.browser.assemble   # + shell.html -> one page
"""
