"""The UI palette: the game's own interface art, grouped and rendered.

    python3 -m allostrias.palette.build      # cache/palette/palette.json
    python3 -m allostrias.palette.assemble   # + shell.html -> one page
"""
